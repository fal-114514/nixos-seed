{ config, pkgs, var, ... }:
{
  # ===========================================================================
  # Shared Modules Aggregator / 共有モジュール集約ファイル
  # ===========================================================================
  # ここに追加したモジュールは、全てのホスト（hosts/*）から自動的に読み込まれます。
  # 実際の有効/無効は、各モジュール内の `mkIf var.xxx.enable` で制御されます。
  # ===========================================================================

  imports = [
    # --- デスクトップ環境 (Desktop Environments) ---
    ./DE/gnome/nixos.nix
    ./DE/kde/nixos.nix
    ./DE/niri/nixos.nix

    # --- システム全体の共有モジュールを追加する場合は、以下にパスを追記してください ---
    # ./examples/service-template.nix
  ];
}
